server:
  port: 8080 # Spring Boot 애플리케이션이 실행될 포트

spring:
  datasource:
    url: jdbc:mysql://34.64.182.217:3306/nextstep?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: nextstep98
    password: Rlaxogns98*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 개발 단계에서만 사용. 운영 시에는 none 또는 validate 권장
    show-sql: true # JPA 쿼리 로그 출력
    properties:
      hibernate:
        format_sql: true # SQL 쿼리 포매팅
  data:
    redis:
      host: 34.47.124.61
      port: 6379
      password: nextstep98
      timeout: 5000ms # 연결 타임아웃 5초 (기본값보다 길게 설정하여 안정성 확보)
      lettuce:
        pool:
          max-active: 8 # 최대 활성 연결 수
          min-idle: 0   # 최소 유휴 연결 수
          max-idle: 8   # 최대 유휴 연결 수
          max-wait: 10000ms # 풀에서 연결을 대기하는 최대 시간
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 5fba457f416463d2a865286e249f2cd0
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            scope:
              - profile_nickname
              - account_email
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
jwt:
  secret: pF4ORt/xd3Ym0UHiJwijgtnYHyNd5B7xQKzSxsWnp5M=
  expiration: 86400000 # Access Token 만료 시간 (24시간, 밀리초)
  refresh_expiration: 604800000 # Refresh Token 만료 시간 (7일, 밀리초)