server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://34.64.182.217:3306/nextstepdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: nextuser
    password: Rlaxogns98*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

jwt:
  secret: YOUR_SECRET_KEY
  access-token-expiration: 3600000       # 1시간 (ms)
  refresh-token-expiration: 1209600000   # 2주

logging:
  level:
    org.hibernate.SQL: debug
